import PropTypes from 'prop-types';
import React from 'react';

let VoteTrack = (props) => {

  return (
    <div>

    </div>
  );
}

export default VoteTrack;

VoteTrack.propTypes = {
  isHost: PropTypes.func,
  questNumber: PropTypes.number
}
